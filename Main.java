public class Main {

    public static void main(String[] args) {
        
        public static string ChekWords() { 
        String a = "Ketchup";
        String b = "Ketchup";
        String c = "Kepchuk";

        if (a.equals(b))
        {
            System.out.println("Слово " + c + " неверно");
        };
        if (a.equals(c))
        {
            System.out.println("Слово " + b + " неверно");
        };
        if (c.equals(b))
        {
            System.out.println("Слово " + a + " неверно");
        };
    }
    // задание 2
    public static float calcSum (int[] numbersArray, int a, int b, int di) {
        int resultA = 0; // числитель итог
        int resultB = 0; // знаменатель итог
        int resultSumma = 0; // Итог

        for (int i = 0; i < 7; i++) {
            resultA += number - a*b;
        }
        for (int i = 0; i < 5; i++) {
            resultB += number - a;
        }
        return (float) (resultA / resultB) * numbersArray[di - 1];
        System.out.println(r);
    }
}
